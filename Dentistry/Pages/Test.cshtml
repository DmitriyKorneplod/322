@page
@model Dentistry.Pages.TestModel
@{
    ViewData["Title"] = "Test";
    ViewData["Value"] = 2 + 2;
}
<p>@Model.GetString()</p>

<table class="table table-striped">
    <thead>
        <tr>
            <td>
                Ид
            </td>
            <td>
                ФИО
            </td>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model.Clients)
        {
            <tr>
                <td>
                    @item.Id
                </td>
                <td>
                    @item.Fio
                </td>
            </tr>
        }
    </tbody>
</table>
